/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.aceternity.com",
      "www.prisma.io",
      "nextjs.org",
      "next-auth.js.org",
      "tailwindcss.com",
      "stripe.com",
    ],
  },
};

export default nextConfig;
